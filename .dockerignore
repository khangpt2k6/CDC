# Keep the build context lean: the image builds the binary from source, so the
# host build artifacts, VCS metadata, logs, and local process docs are not needed.
bin/
dist/
*.exe
*.log
.git/
.github/
docs/
deploy/grafana/
deploy/prometheus/
*.md
